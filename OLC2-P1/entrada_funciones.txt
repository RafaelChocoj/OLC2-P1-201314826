

fn main() {
   
    struct Personaje {
    nombre: String,
    edad: i64,
    descripcion: String
    }
// Struct
struct Carro {
    placa: String,
    color: String,
    tipo: String,
    perso: Personaje
    }

let mut p1 = Carro {
placa:"palc".to_string(),
color:"red".to_string(),
tipo:"nose".to_string(),
perso: Personaje { nombre:"Fer".to_string(), edad:18,
descripcion:"No hace nada".to_string()}
};

println!("{:?}", p1);
}